<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - WebChat</title>
    <link href="../partials/styles/output.css" rel="stylesheet">
    <style>
        #profile img {
            border-radius: 100%;
        }

        #addRoom {
            border-radius: 100%;
        }

        #updateForm {
            opacity: 0;
            z-index: 10;
            backdrop-filter: blur(10px);
        }

        .transition {
            opacity: 1 !important;
            transform: translateX(15%) !important;
            transition: all 0.8s !important;
        }
    </style>
</head>


<body class="w-screen h-screen flex flex-col items-center bg-black text-white">
    <%- include('../partials/components/navbar')%>
        <section class="border-2 border-white-700 w-5/6 h-5/6 flex flex-col relative rounded-2xl">
            <p class="text-2xl font-bold m-4 mb-5">PROFILE</p>
            <div id="profile" class="flex gap-6 pl-8">
                <img id="avatar" class="w-16 h-16 sm:w-32 sm:h-32 border-1 border-white object-cover cursor-pointer"
                    src=<%=avatar%>>
                <div class="flex flex-col mt-4">
                    <p class="font-bold text-2xl sm:text-3xl" id="name">
                        <%= uname %>
                    </p>
                    <p class="font-bold text-blue-400 ml-4">
                        #<%= username %>
                    </p>
                </div>
                <a href="/update" class="mt-6 ml-8 text-md text-blue-700 font-bold cursor-pointer" id="change">
                    Change</a>
            </div>
            <div id="rooms" class="my-8 max-h-[55%] pl-8">
                <p class="font-bold text-2xl sm:text-3xl pl-8">Live Rooms</p>
                <section class="groups w-max ml-24 mt-4">
                    <ul class="flex gap-y-12 gap-x-6 w-full h-max flex-wrap justify-center overflow-auto"
                        id="live_rooms">
                        <% for (const room of rooms) {%>
                            <li class="cursor-pointer rounded-full w-max">
                                <section class="relative flex justify-center items-center">
                                    <div class="group flex justify-center transition-all bg-transparent">
                                        <a href=/chat/<%=room%>>
                                            <p
                                                class="w-max h-12 grid place-items-center font-bold text-2xl border border-white cursor-pointer capitalize px-4 py-2 rounded-lg">
                                                <%=room%>
                                            </p>
                                        </a>
                                        <div id="greendot"
                                            class="w-3 h-3 bg-green-400 rounded-full absolute right-0 opacity-0"></div>
                                    </div>
                                </section>
                            </li>
                            <%}%>
                    </ul>
                </section>
            </div>
            <div class="absolute bottom-5 flex gap-x-12 left-1/2 -translate-x-1/2">
                <button id="gotochat" class="bg-[#32aa34] py-2 px-8 font-bold text-xl rounded-xl w-max">Chat</button>
                <a href="/logout">
                    <button id="logout" class="bg-red-700 py-2 px-8 font-bold text-xl rounded-xl w-max">Log
                        Out</button>
                </a>
            </div>
        </section>
        <script src='/profile/script.js'></script>
</body>

</html>